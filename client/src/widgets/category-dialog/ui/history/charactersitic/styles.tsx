import { styled } from "@mui/material/styles"

export const CharacteristicEventContainer = styled("div")(() => ({
  display: "flex",
  gap: 8,
  alignItems: "center",
  fontSize: 12,
}))

export const CharacteristicEventsBox = styled("div")(() => ({
  display: "flex",
  gap: 2,
}))
