import { Image as ImageBase } from "shared/ui/image"
import styled from "styled-components"

export const StyledPhoto = styled(ImageBase)`
  width: 100%;
  height: 250px;
  object-fit: cover;
  border-radius: 8px;
`
