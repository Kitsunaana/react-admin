import { styled } from "@mui/material/styles"

export const Root = styled("form")(() => ({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  flexGrow: 1,
  gap: 8,
  padding: "8px 0px",
}))
