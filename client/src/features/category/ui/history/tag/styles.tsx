import { styled } from "@mui/material/styles"

export const TagEventContainer = styled("div")(() => ({
  display: "flex",
  gap: 8,
  alignItems: "center",
  fontSize: 12,
}))

export const TagEventsBox = styled("div")(() => ({
  display: "flex",
  gap: 2,
}))
