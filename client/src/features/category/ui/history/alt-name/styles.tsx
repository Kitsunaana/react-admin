import { styled } from "@mui/material/styles"

export const AltNameEventContainer = styled("div")(() => ({
  display: "flex",
  gap: 8,
  alignItems: "center",
  fontSize: 12,
}))

export const AltNameEventsBox = styled("div")(() => ({
  display: "flex",
  gap: 2,
}))
