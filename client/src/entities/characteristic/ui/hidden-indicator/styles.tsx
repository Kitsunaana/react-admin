import { styled } from "@mui/material/styles"
import { Box } from "shared/ui/box"

export const Root = styled(Box)(() => ({
  display: "flex",
  alignItems: "center",
  flexDirection: "row",
  marginRight: 8,
}))
