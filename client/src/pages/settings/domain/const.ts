import { DefaultAppSettings } from "shared/lib/app-settings"

export const defaultLanguage = "ru"

export const defaultTheme = "light"

export const defaultIconSettings = {
  weightIcon: 200,
  fillIcon: 0,
}

export const defaultSettings: DefaultAppSettings = {
  language: defaultLanguage,
  theme: defaultTheme,
  iconSettings: defaultIconSettings,
}
